// splitting code into multiple files, Clean code: reusable

const people = ["Njox", "Glory", "Jasmine"];

module.exports = people;

// exporting multiple module
/**
  module.exports = {
  people: people
  }

A Nice way to import multiple things into the project is through the use of destructuring.


os module
os.platform
 */
